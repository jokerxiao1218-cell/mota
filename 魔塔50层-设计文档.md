# 魔塔50层(Pygame 复刻)设计文档

> 创建时间:2026-09-06 | 状态:待真机验证(batch 1~7 全部 ✅:187 测试绿 + 全塔无头走查通关;剩 §8 运行验证清单由用户执行,勾完改"✅ 已交付")

## 1. 需求卡(已确认)

- **背景**:全新项目,在 `~/mota50` 从零复刻经典《魔塔50层》。原版考据结论:它的原身是日本游戏《Tower of the Sorcerer》(TSW)的汉化版,**不是**国内"魔塔 v1.12/24层"改编线(那条线的怪物体系不同,不采用)。技术栈:Python 3.10 + Pygame(用户已选定)。目的:做一个完整可玩、忠于原版数值的爬塔游戏,顺便学"数据驱动 + 纯函数规则 + 事件系统"这套游戏架构。
- **目标**:方向键走格子 → 撞怪自动回合战斗(打得过直接打,打不过撞不动)→ 攒钥匙开门、宝石加属性 → 50 层爬到顶击败魔王芝诺通关。
- **输入/输出**:
  - 键位:`方向键 / WASD` 移动 | `空格 / 回车` 推进对话、确认选择 | `H` 怪物手册(查看+预测伤害) | `Esc` 菜单(存档/读档/退出)
  - 画面:Pygame 窗口,11×11 格子地图(原版尺寸),旁侧状态栏(生命/攻击/防御/金币/三色钥匙/当前层)
  - 开局流程(源码考古终审修正,见 §3.3):持神圣剑盾以 生命1000/攻100/防100 从第 1 层(5,10) 出发;走到第 3 层踩格触发事件1(魔王伏击夺装备)后变为 400/10/10 落到第 2 层——原版的开场动画在本数据里是可玩流程,我们跟数据走
- **约束**:
  - 依赖:只用 `pygame` + `pytest`(已装入项目 venv,免 sudo 方案见 §2),不再引入任何第三方库
  - 版权:地图/事件数据转换自 MIT 协议仓库并保留出处(`reference/README.md`);美术素材**全部程序自绘**(运行时生成像素贴图,零外部素材);仅个人学习,不公开分发、不商用
  - 错误处理:存档文件损坏→清晰报错并拒绝读档,不许崩;数据文件缺失→启动时报"缺哪个文件";打不过的怪**禁止移动进入**(原版行为);未知事件类型→明确提示而非静默跳过
  - 改动范围:只动 `~/mota50`,不碰其他项目
  - 性能:60 FPS(11×11 小地图无压力);移动逐格瞬间结算,不强制等待动画
  - 提交习惯:每小步验证通过自动 commit(用户已确认)
- **验收标准**:规则层单测全绿(第 5 节)+ 实际运行全流程手动清单(第 8 节)
- **本项目无实体硬件,"真机验证" = 实际运行游戏逐项手动验证**(沿用 tank-battle 惯例)

## 2. 现场调查

- **环境**:Ubuntu 22.04 / Python 3.10.12 / git 2.34.1;pygame 2.6.1 + pytest 9.1.1 已装入 `.venv/`
- **venv 坑(重要,以后装包都这么装)**:系统缺 `python3-venv`(要 sudo),所以用 `python3 -m venv --without-pip` 建环境、`python3 -m pip install --target .venv/lib/python3.10/site-packages 包名` 装包,免 sudo,效果与标准 venv 一致
- **惯例参照**:tank-battle 同流程项目——游戏=单独目录、设计文档放根下、无硬件则以运行清单代替真机验证、参考素材拷进项目并注明出处
- **最相似既有实现**:
  - 同技术栈:[oscarcx123/MagicTower-Python](https://github.com/oscarcx123/MagicTower-Python)(pygame,BSD-3)——引擎/数据分离、300 行事件解释器、JSON 全量存档
  - 同目标体量:[orochitian/mota](https://github.com/orochitian/mota)(约 1800 行 JS 跑通完整 50 层,证明"初学者手写 50 层"可行)
  - 数据来源:[tacthgin/MagicTower](https://github.com/tacthgin/MagicTower)(MIT)——唯一完整的 50 层 1:1 复刻数据,副本已在 `reference/tacthgin/`
- **新代码分层**(全新项目自建四层):数据(纯 JSON)→ 规则(纯函数,不碰 Pygame)→ 引擎(循环/渲染/输入)→ 界面(状态栏/对话/手册)

## 3. 业界调研

### 3.1 原版考据(双路调研结论,数值权威性见附录 A)

- **战斗公式**(玩家先手、全程无随机数,经 tswKai 逆向源码 + 原版 exe 交叉验证):
  ```
  d1 = effATK - mDEF            # 玩家每击伤害
  d2 = max(0, mATK - DEF)       # 怪物每击伤害,下限 0(不会"保底1点")
  effATK = ATK × 2  仅当:持十字架且怪 ∈ {兽人, 兽人武士, 吸血鬼};
                       或持屠龙匕且怪 = 魔龙;否则 = ATK
  破防判定用【裸 ATK】:ATK <= mDEF → 整场不可战斗(×2 也不算,原版怪癖,保留)
  n = (mHP - 1) // d1 + 1       # 玩家出手次数 = ceil(mHP / d1)
  玩家总损血 = (n - 1) × d2      # 玩家先手,怪只反击 n-1 次
  可战胜条件:总损血 < 当前 HP;战后金币 += mGold ×(持幸运金币 ? 2 : 1)
  ```
- **区域倍率**:area = floor(楼层 ÷ 10) + 1,即 1–10层×1 … 41–50层×5。红瓶 HP+50×area,蓝瓶 HP+200×area,红宝石攻+1×area,蓝宝石防+1×area
- **祭坛商店**(4/12/32/46 层,三选一,全塔共享购买次数 n):价格 = 10×(n²−n+2) 即 20/40/80/140/220/320…;买生命 +100×n,买攻 +2×祭坛区,买防 +4×祭坛区
- **特殊怪物机制**:魔法警卫被两个(相隔2格)上下或左右夹住中间格→踏入则 HP = ceil(HP/2);初级/高级巫师不可战斗,走到其相邻格固定扣 100/200(可叠加,受击后巫师瞬移到对称格),持神圣盾免疫;双手剑士=数值双攻(680)而非机制双击;49 层破"骰子5"魔法警卫阵→假魔王数值×0.1
- **50 层魔王两套数值**:原版显示 8000/5000/1000(剧情上不可战胜);汉化版真身 5000/1580/190(可战胜通关)——两者都做:剧情战→真身战
- **存档设定**:原版规定需持有怪物手册(3 层老人赠送)才能存档
- **二周目**(怪物数值×44):不做;**先攻怪**:**做上**(2026-09-06 用户重新拍板,理由见 §9)
- **版权结论**:原版为自由软件(无书面许可);MIT 数据可用需署名;个人非商用学习复刻风险低;素材自绘;不公开分发

### 3.2 架构参考(来源+借鉴点)

| 来源 | 借鉴点 |
|---|---|
| [ckcz123/mota-js](https://github.com/ckcz123/mota-js)(BSD-3,社区标准,588★) | 引擎/数据物理分离;事件=动作列表+解释器(它的 130+ 种动作我们裁到 ~12 种起步) |
| [tacthgin/MagicTower](https://github.com/tacthgin/MagicTower)(MIT,50层复刻) | `CalculateSystem` 纯函数战斗,计算/表现/应用三层分离——战斗函数照此思路 |
| [oscarcx123/MagicTower-Python](https://github.com/oscarcx123/MagicTower-Python)(BSD-3,同栈) | 300 行事件解释器(初学者可读的蓝本);JSON 全量快照存档 |
| [orochitian/mota](https://github.com/orochitian/mota) | 约 1800 行跑通 50 层——体量标杆,不害怕 |
| 反面教材:gdut-yy(Java)/ckcz123 旧版(C++) | 数值硬编码在巨型 switch 里——我们坚决数据驱动 |

结论:取 mota-js 的"数据驱动+事件流"骨架(简化版)+ tacthgin 的纯函数战斗 + Python 样板的 JSON 快照存档。不照搬 mota-js 全家桶(2.6 万行是给不会编程的人造塔的引擎级产品,不是我们要的)。

### 3.3 源码考古终审(2026-09-06,对 §3.1 的勘误与补全)

tacthgin 引擎 TypeScript 源码 + TMX 层属性解码完成,以下结论已写进数据(转换脚本 v4)并同步给各并行智能体:

| 事项 | 终审结论 |
|---|---|
| 楼层编号 | tmx 编号=游戏楼层号;0 层=地下室(向下飞行器进入,藏幸运金币) |
| 区域倍率 | **area=(floor−1)//10+1**(10/20/30/40 层分别是×1/×2/×3/×4;先前版本边界错一档,已改) |
| 祭坛 | zone=floor//10,攻=(zone+1)×2、防=(zone+1)×4;价格 10×(n²−n+2) 与原版一致;摆放 4F@5 / 12F@93 / 32F@108 / 46F@5 |
| 门语义 | 1004=事件生成门;1005=杀光守卫开门;**1006=墙门(直接撞开)**;wall 层 gid87=普通墙(镐/地震可拆) |
| 大怪 | 大乌贼/魔龙 3×3 **整体单场战斗**(非 9 部位);战斗元素格=贴图格+1 行 |
| 克制道具 | monster.extraDamage 实为【道具 id】(28=十字架,29=屠龙匕),已转 counter_item 字段 |
| 魔法伤害 | 巫师相邻/警卫夹击**只在 ≥40 层生效**(3 层开场挨打是纯演出);若会被巫师电死,移动被拒绝 |
| 楼梯 | location=[上站位,下站位,上层差(默认+1),下层差(默认-1)];上楼落【新层下站位】、下楼落【新层上站位】;43 层上梯+2、45 层下梯−2(双双绕过 44 层);隐藏楼梯踩了无效但"站在楼梯旁"判定有效 |
| 事件触发四源 | ①踩格(event 层属性)②杀怪(monster 层属性:杀光列表触发;或"杀光A且B全存活"触发、先杀B任一则永久取消)③怪种 eventId(死亡时)④NPC 对白后;事件 save=N 且 N≠当前层 → 进 N 层才执行(跨层挂起) |
| 49 层封印 | 杀十字位(16,26,28,38)且四角(15,17,37,39)全存活 → 假魔王三围×0.1(事件21);先杀四角任一 → 封印永久失效 |
| 结局链 | 26F 公主→事件26→24F 传送格→事件27 jump 50F→50F 小偷→事件25(真魔王133出现)→杀 133 通关;**无仙子评分**(数据无此内容,移出范围) |
| NPC | 摆放=npc 层属性(39 个全接线);小偷 wall=对话后开的门、move=依次走的位置,走完消失+触发 event |

**先攻怪新证据→已拍板**:40 层 12 只怪按位置标注先攻(鬼战士×3/战士×3/双手剑士×3/骑士×3,损血=n×d2 而非 (n−1)×d2)。用户此前因"无权威来源"确认不做;考古终审找到确凿数据后,2026-09-06 用户重新拍板**做上**——calc_battle 的 first_attack 开关启用,由引擎按楼层 first_attack 位置表传入。

**tacthgin 数据坑与移植处置**:
1. 事件11/12 引用不存在的怪 135 → 保持原样(appear 遇缺跳过+日志)
2. 事件4 `show 15` 是 115 笔误 → **转换脚本已修**
3. 幸运金币×2 因键名写错在原实现中失效 → 我们按原版实现 ×2
4. 镐/向上飞行器全图无获取途径(死道具,数据保留)
5. 商店进度不存档(原实现 bug)→ 我们的存档含 altar_count,已修复
6. npc31 未摆放、小偷1 第二句台词不可达 → 无害,保留

## 4. 设计方案

### 4.1 目录结构

```
~/mota50/
├── 魔塔50层-设计文档.md   ← 本文档(进度唯一事实来源)
├── run.sh / test.sh       ← 一键运行 / 一键测试(内部用 .venv)
├── game/
│   ├── main.py           # 入口:拼装数据层→规则层→引擎→界面
│   ├── core/             # 规则层【禁止 import pygame,无显示也能跑单测】
│   │   ├── battle.py     #   战斗纯函数 calc_battle()
│   │   ├── pickups.py    #   拾取/宝石/血瓶/祭坛结算
│   │   ├── special.py    #   巫师相邻伤害、警卫夹击、封印等特殊机制
│   │   └── state.py      #   游戏状态(HP/攻/防/金币/钥匙/道具/flags)
│   ├── data/             # 数据层(全 JSON,由转换脚本生成)
│   │   ├── monsters.json #   33 怪(权威数值表,附录 A)
│   │   ├── items.json    #   道具表(附录 B)
│   │   ├── tiles.json    #   图块 id → 类型/贴图/可通行性
│   │   └── floors/f1..f50.json  # 每层:11×11 地图 + 事件 + 楼梯
│   ├── events.py         # 事件解释器(动作列表 → 逐条执行)
│   ├── engine.py         # 引擎层:Pygame 循环/键盘输入/渲染调度
│   └── ui.py             # 界面层:状态栏/对话框/怪物手册/菜单/像素贴图生成
├── scripts/
│   └── convert_tacthgin.py  # 一次性:reference/tacthgin → game/data JSON
├── tests/                # pytest 单测(只测 data/core/events 三层)
├── reference/            # 参考数据副本(MIT,出处见其 README)
└── save/                 # 玩家存档(gitignore)
```

### 4.2 关键设计选择(每条配"为什么")

| # | 选择 | 为什么 |
|---|---|---|
| 1 | 全数据驱动:地图/怪物/道具/事件全是 JSON,代码只做引擎 | 50 层=填表,不撑爆代码;加一只怪=加一行数据(mota-js 已验证这条路) |
| 2 | 战斗=纯函数 `calc_battle(hero, monster, flags) → {可打否, 玩家损血, 回合数}`,**不改任何游戏状态** | 最值得单测的模块;同一个函数复用为怪物手册的预测面板(tacthgin 三层分离);十字架/幸运金币等开关**显式入参**,比 mota-js 埋在全局状态里更可测 |
| 3 | 事件=动作列表+解释器,首版约 12 种动作:`text / choices / shop / give_item / set_block / change_floor / teleport / weaken_monster / set_flag / if / ending / sound` | 对话、祭坛商店、49 层封印、44 层飞行全部走这一套(mota-js 130 种的裁剪版,以 Python 样板 300 行解释器为蓝本) |
| 4 | 存档=JSON 全量快照(勇士属性+每层地图变化+flags),`save/save_1~3.json` 三槽;**拿到怪物手册前不能存档(原版设定)** | JSON 可读可手改可单测(Python 样板范本);原版设定忠实保留 |
| 5 | 特殊怪能力用**命名字段**(`"adjacent_damage": 100`、`"halve_hp_trap": true`),不用数字编号 | 可读性优先(tacthgin 风格),单测/调试直观 |
| 6 | `core/` 层**禁止 import pygame** | 规则层与渲染彻底分离,无显示环境(SSH/CI)也能跑全部规则单测 |
| 7 | 像素贴图**运行时程序生成**(16×16 像素画:砖纹墙/钥匙/史莱姆…),不引入任何外部图片 | 零版权风险地满足"经典像素"观感;将来想换真贴图只改 `tiles.json` 映射,引擎不动 |
| 8 | 地图 11×11 数字 id 二维数组(原版 121 格),一格一物 | 原版尺寸;tacthgin TMX 转换而来;序列化与存档都简单 |

### 4.3 风险点与对策

| 风险 | 对策 |
|---|---|
| TMX→JSON 转换:tacthgin 的 .tmx 编号 0~50 与游戏楼层号的对应关系(0.tmx 是"第1层"还是特殊层)待核对 | batch 1 转换时用楼梯连接关系+事件对白交叉核对,校验写进单测(每层有入口、楼梯两端互指) |
| tacthgin 怪物数值有 3 处金币抄录错误 | 转换时以附录 A(exe 提取权威表)为准逐项断言,33 怪数值钉死在单测里 |
| 44/49/50 层特殊机关事件复杂 | 先保证 batch 4 事件解释器表达力(weaken_monster/teleport/if 已备),batch 6 再接入 |
| 原版怪癖(破防判定用裸攻击、警卫夹击向上取整)易被"顺手修正" | 单测钉死 + 文档声明:忠实原版,不改 |
| 像素贴图全靠程序生成,首次观感可能朴素 | batch 3 先出基础版,batch 7 打磨;不满意只改贴图生成函数,不碰架构 |

## 4.4 模块接口契约(2026-09-06 新增:应用户要求,batch 2/3/4/5 多智能体并行开发)

> **并行规则**:跨模块只传【纯 dict / list / int / str / bool】,禁止跨模块 import 对方的类——
> state 就是普通字典(天然可 JSON 存档),也让模块间零编译依赖,天然支持并行+模拟测试。
> 文件所有权互斥:规则智能体只动 `game/core/` 与 `tests/test_core.py`;引擎智能体只动
> `game/engine.py、ui.py、main.py、assets.py` 与 `tests/test_engine.py`;事件智能体只动
> `game/events.py` 与 `tests/test_events.py`。禁止动他人文件/设计文档/数据文件,禁止任何 git 操作;
> 发现契约有漏洞 → 停下在报告里说明,不许自行偏离。各自只跑自己的测试文件。

### A. 游戏状态 state(纯字典,存档=整包 JSON)
```python
{
  "floor": 1,                        # 当前楼层号(1-50;0=序章层)
  "hero": {
    "hp": 1000, "attack": 100, "defence": 100, "gold": 0,
    "keys": {"yellow": 0, "blue": 0, "red": 0},
    "props": {"18": 1},              # 道具id(str)→数量;工具恒1,用完删键
    "sword": "12", "shield": "17",  # 已装武器/盾的道具id(存字符串,与 props 键惯例一致);序章自带神圣剑/神圣盾,3 层被夺走(事件1)
    "pos": [5, 10]                   # [x, y],x 向右 y 向下,均 0-10
  },
  "flags": {
    "altar_count": 0,                # 祭坛全局购买次数(价格公式 10×(n²−n+2))
    "events_done": [],               # 已触发过的一次性事件 id
    "monsters_dead": []              # "floor:x,y" 记录,恢复楼层地图用
  },
  "floors_state": {"1": [[x, y, 新栈或null], ...]},  # 每层相对初始数据的变化
  "visited": [1]                     # 到过的楼层(飞行魔杖目标)
}
```
### B. core/ 规则层签名(规则智能体:batch 2 + 5)——已实现并按勘误定稿
- `new_state() -> dict` — 序章开局:HP 1000/攻 100/防 100/金 0,自带神圣剑"12"/神圣盾"17",floor=1、pos=[5,10]、visited=[1];3 层事件 1 被夺走→400/10/10(事件层改写)
- `area(floor) -> int` — `(floor-1)//10+1` 区域倍率(整十层边界已按 TS 源码修正:10/20/30/40 层分别 ×1/×2/×3/×4)
- `calc_battle(hero, monster, flags) -> {"can_fight","hero_damage","turns","gold"}` — **纯函数**;monster 为 monsters.json 条目;flags={"cross","lucky_coin","dragon_slayer","first_attack"}(bool,默认全 False);公式严格按 §3.1(玩家先手、破防判定用裸攻击、损血=(n−1)×d2;**first_attack=True 时损血=n×d2**,由引擎按楼层 first_attack 位置表传入——2026-09-06 用户拍板启用);巫师 125/126 按数据 special.unfightable 判不可战
- `apply_pickup(state, item_id, data) -> state` — 血瓶宝石×area、武器盾只升不降补差额、钥匙计数、工具进 props 恒 1;原地修改并返回
- `use_tool(state, item_id) -> state` — 圣水:HP += 攻+防,用完删 props 键;其他道具归引擎层(地图/传送),传入抛 ValueError
- `altar_price(n) -> int`、`altar_buy(state, choice) -> state` — choice ∈ "hp"/"attack"/"defence";买生命 +100×n,买攻 +2×(floor//10+1)、买防 +4×(floor//10+1)(**祭坛区与宝石区公式在整十层不同**:第 10 层宝石×1 但祭坛攻+4,TS 源码如此,已专门测试钉死);钱不够抛 ValueError 且零副作用
- `adjacent_damage(state, floor_dict, x, y) -> int` — 巫师相邻魔伤合计(125→100、126→200,可叠加);持神圣盾=0(判定以 hero.shield=="17" 为准,core 兼容 props 旧写法);**40 层以下恒 0**
- `guard_trap(state, floor_dict, x, y) -> state` — 警卫夹击:勇士贴中间格,(x±1,y) 或 (x,y±1) 两侧都是警卫 130 → HP=(HP+1)//2;**40 层以下不触发**
- `save_game(state, slot)` / `load_game(slot) -> dict` — save/save_N.json 三槽;坏 JSON/缺文件/版本不符/缺关键字段抛 SaveError(带文件名,缺字段一次报全);**"需怪物手册才能存档"门槛由引擎菜单层判 props 有无 "18"**(原版设定)
### C. events.py 事件解释器(事件智能体:batch 4)——已实现并按勘误定稿
- `class EventRunner(event, state, api, rules=None, data=None)`;`step() -> intent`;`feed(response)`
  - intent:`{"op":"chat","lines":[...]}` / `{"op":"choices","options":[...]}` / `{"op":"pending","floor":N}`(事件存档层≠当前层,跨层挂起,进层时重演) / `{"op":"done"}`
  - feed:chat 推 None;choices 喂 0 起的选择序号 —— **模拟测试=喂 canned 响应推进,全程无显示**
- 动作类型以 `game/data/events.json` 物化结果为准(17 种,照参考实现考据);payload 里的数字是**展平一维坐标(0-120)**:x = pos%11,y = pos//11
- `NpcFlow`(NPC 对话/买卖/祝福×1.03/送道具/接力事件,小偷开暗道)、`AltarFlow`(三选一菜单,价格走 rules)、`use_tool` 10 件道具(魔杖/上下翼/中心飞行器/镐/地震卷轴/冰冻/炸弹/魔法钥匙/圣水/记事本)
- **api 适配器 16 键(引擎注入,签名以 game/events.py docstring 为准)**:`get_floor/set_cell/spawn_monster/add_npc/remove_npc/add_trigger/remove_trigger/move(展平坐标)/weaken/register_monster_door(含在门位摆 1004)/collide/jump_floor(x,y 传 None=楼梯口落点)/show/clear_npc_event`;**约定:事件/道具的怪物格增删流经 set_cell,引擎在此挂钩 monsters_dead 记账**
- 新增 state 约定(向后兼容):`flags.npcs_done`(一次性 NPC)、`flags.npcs_event_talk_cleared`、`state.records`(记事本对白,持道具 19)、祝福后 attack/defence 为浮点
### D. engine.py + ui.py(引擎智能体:batch 3;机关接线:batch 6)
- 引擎层:pygame 主循环(输入→调 core/events→渲染);测试用 `SDL_VIDEODRIVER=dummy` 无头跑
- 地图格是**叠放栈**:floor 文档 `grid[y][x]` = null 或自下而上的列表;`stack[-1]` 是当前可交互对象(墙门下可能压着道具——开门/破墙后弹出,露出下层,这是原版机制)
- ui 层:状态栏/对话/怪物手册/菜单;像素贴图运行时生成(16×16),只认 kind 与 id,不依赖 sprite 名
- 撞怪:调 `calc_battle` 预判,不可打→移动拒绝;可打→结算
- **机关语义(batch 6 物化,上游 ParserFactory/DoorSystem/MoveSystem 考据)**:
  - `hide`(格子标记)= 幽灵格:不可见、**不挡路**、不可交互,事件 show 显现(10 层隐藏上梯、33 层下梯上的隐形黄门、41 层墙后巫师)
  - `appear`(门格标记)= 隐形墙:**挡路**、撞一下显形、显形后永远是墙(23 层迷宫 43 面、33 层暗墙);楼层 `appear_event`:全撞现→触发事件(23 层→事件 8 挂 29 层)
  - `passive`(门格标记)= 撞不开的墙门;解除途径:小偷挖(events 层)/ 楼层 `door_unlocks`(杀怪解锁,f41)/ 守卫门列表
  - 守卫门(guard_doors)**不看门 id**:数据里守卫门 1004/1006 居多、1005 仅 2 层,按 id 过滤会让机关失效(已修复)
  - 楼层 `disappear_event`(f39):开钥匙门记账——开错=永久作废、开齐=触发事件;进度记 flags
  - 楼层 `wall_shows`(f41):撞开假墙→显现指定格(墙后巫师)
  - `monster_move`(怪格标记,f47 巫师):吃魔伤时镜像瞬移到以勇士为中心的对称格
  - 守卫门【事件内嵌版】(上游 event.json 动作 `monsterDoor:{怪位csv: 门位列表}`,转换 v6 物化为楼层 guard_doors 条目,复用引擎既有弹门机制零改动):杀光一组怪→指定门自动弹开,不看门 id——全塔 3 处(f10 事件2 埋伏:8 骷髅→顶行门、f20 事件6 决斗:(5,0) 双卫→三门、f33 事件13)
  - 跨层挂起:事件 meta.save≠当前层 → pending 登记,进层钩子重演(23 层迷宫→29 层小偷解锁链)
  - 动态摆放持久化:`state.placements` 存该层 NPC/触发器全量快照(存档不丢)

## 5. 测试计划(用例即需求的精确定义,请重点审)

单测跑法:`./test.sh`(venv 内 pytest),预期全部通过。只测 data / core / events 三层(规则不依赖渲染)。

**战斗纯函数(数字全部来自附录 A 权威表)**:
- 正常胜:勇士 400/120/35 撞绿色史莱姆(35/18/1)→ 出手 1 次,损血 0
- 多回合:同上勇士撞兽人武士(320/120/15)→ 每击 105、出手 4 次、损血 3×85=255(<400 胜)
- 不破防:ATK 10 ≤ 石头人防 68 → 返回"不可战斗",移动被拒
- 0 伤害:DEF ≥ 怪攻(防御堆过小蝙蝠的 38)→ 怪每击 0,损血 0 仍胜
- 先手优势:普通怪总损血 = (n−1)×d2,不是 n×d2
- 先攻怪(2026-09-06 用户拍板做上):40 层先攻位置(楼层 first_attack 表)撞鬼战士,总损血 = n×d2,比非先攻位置多一刀;非 40 层/普通位置行为不变
- 十字架:对吸血鬼攻击×2 生效;但破防判定仍用**裸 ATK**(原版怪癖,即使 ×2 后能破防也判不可战斗)
- 幸运金币:战后金币 ×2
- 边界:d1 恰好整除 mHP(n 不多不少)、怪防=勇士攻(ATK==mDEF 也判不可破防)

**祭坛**:第 1/2/3/4 次价格 = 20/40/80/140(全塔跨楼层共享计数);12 层祭坛买攻 +4、买防 +8(区域2);买生命第 n 次 +100×n;钱不够拒绝且不耗次数

**拾取与区域倍率**:第 1 层红宝石攻+1,第 41 层红宝石攻+5;圣水 HP += 攻+防(一次性);武器/盾按附录 B 定值

**特殊机制**:初级巫师相邻扣 100、两只相邻叠加 200;持神圣盾免疫;魔法警卫夹击 HP=ceil(HP/2)(999→500,奇数向上取整);49 层封印后假魔王 HP/攻/防 ×0.1(8000→800)

**钥匙与门**:无钥匙撞门不动、不消耗;开黄门耗 1 黄钥匙;魔法黄钥清空本层全部黄门

**存档**:存档→杀怪→读档 = 怪复活、状态回滚一致;手工写坏 save JSON → 读档抛出带文件名的清晰错误,不崩

**数据完整性(转换脚本的验收)**:50 层 JSON 全部可加载;每层坐标合法、含入口楼梯;楼梯两端互指(上楼的下落点=本层的上楼梯位置关系正确);33 怪数值与附录 A 逐项断言;事件里引用的怪物/道具/图块 id 全部存在(无悬空引用)

**事件解释器**:text 逐条推进;choices 分支选择;shop 扣钱加属性、金币不足拒绝;weaken_monster 修改目标数值

## 6. Batch 划分与进度(每个 batch 做完必须:测试全绿 + commit)

| # | 目标(做完后系统状态) | 涉及文件 | 验证方式 | 状态 |
|---|---|---|---|---|
| 1 | 数据管线:转换脚本把 tacthgin 参考数据转成 monsters/items/tiles/floors 全量 JSON;数据加载器+完整性校验 | scripts/convert_tacthgin.py、game/data/、tests/ | pytest 全绿(含 50 层加载、33 怪数值断言、楼梯连通校验) | ✅ |
| 2 | 规则核心:战斗/祭坛/拾取/特殊机制纯函数,零 pygame 依赖 | game/core/、tests/ | pytest 全绿(第 5 节规则用例全过) | ✅(61 测试) |
| 3 | 引擎基础可玩:窗口/循环/输入/像素贴图生成/渲染/移动/撞怪(调 core)/楼梯换层/钥匙门/宝石血瓶/状态栏——能实际玩前几层 | game/engine.py、ui.py、main.py、assets | 手动玩前几层 + 冒烟单测 | ✅(51 测试,含先攻怪端到端与无头主循环冒烟) |
| 4 | 事件系统+NPC:解释器全套动作、对话/选择支/祭坛商店/老人商人小偷、怪物手册(预测面板,复用 calc_battle)、飞行魔杖 | game/events.py、ui.py、data 事件 | 解释器单测 + 手动过剧情层 | ✅(43 测试,零 pygame) |
| 5 | 存档系统:三槽 JSON 快照存/读、需怪物手册设定、坏档处理 | game/core/state.py、ui.py | 存读档单测 + 手动存/读 | ✅(并进 batch 2 智能体) |
| 6 | 集成:真 core/events 接进引擎已毕,剩全塔接线——祭坛格进 AltarFlow、kill_triggers 消费(含 keep_alive 永久取消)、动态摆放进存档(placements)、44 层异空间/中心飞行器、49 层骰子封印、50 层剧情战→真身战、结局链、巫师镜像瞬移、道具使用键位、11 种机关语义(hide/appear 等,侦察中)、全塔无头模拟走查 | engine.py、main.py、events.py、data | 走查脚本全塔通关 + 手动抽查 16/25/44/49/50 层 | ✅(walkthrough 全塔通关,187 测试) |
| 7 | 打磨收尾:README、第 8 节运行清单全过、异常路径、性能确认 | 全部 | 第 8 节清单全部勾选 | ✅(开发侧;用户真机清单待执行) |

## 7. 执行记录

| 日期 | Batch | 结果 | 备注(Commit / 问题) |
|---|---|---|---|
| 2026-09-06 | — | 环境就绪 | `2d44d62` init: 骨架+venv+参考数据 |
| 2026-09-06 | — | 设计确认 | `f4b2303` 设计文档 v1;用户逐项确认方案/范围(二周目、先攻怪不做)/测试计划/批次划分 |
| 2026-09-06 | — | 流程变更 | 应用户要求:batch 2/3/4/5 改为多智能体并行(§4.4 契约),batch 6/7 集成阶段串行 |
| 2026-09-06 | 1 | 数据管线主体 ✅ | 转换脚本+加载器+51楼层+34怪(权威校正)+NPC/触发接线;10 测试绿;层属性(hide/door机关/stair落点/firstAttack)待语义报告接线 |
| 2026-09-06 | 1 | 语义接线 ✅ | 源码考古终审→转换 v4:stair_links/kill_triggers/guard_doors/first_attack/altar/counter_item/门语义 1004/1005/1006;show 15→115 勘误;开局流程/区域倍率边界修正(§3.3);11 测试绿 |
| 2026-09-06 | 2+5 | 规则核心+存档 ✅ | 智能体交付 core/ 四模块(battle/pickups/special/state),tests/test_core.py 61 条全绿;契约按实现定稿(§4.4-B,含警卫夹击几何、魔伤 40F 门槛、存档门槛归引擎);序章开局/区域公式勘误已落实。备注:并行期间主线误用 `git add -A`,game/core/* 与 test_core.py 分别被卷进 `1ec5918`/`bbf0eea` 提交,功能无损,此后改按路径提交 |
| 2026-09-06 | — | 范围变更 | 先攻怪:考古终审在 40 层找到 12 只确凿数据,用户重新拍板**做上**(推翻此前"不做");calc_battle 的 first_attack 开关启用,引擎按楼层 first_attack 位置表传 flags |
| 2026-09-06 | 4 | 事件系统+NPC ✅ | 智能体交付 events.py(解释器 17 动作/NPC 流/祭坛流/10 道具/手册预测),tests/test_events.py 43 条全绿,主线复验 115 条绿+零 pygame;api 适配器 16 键定稿(§4.4-C);4 条语义存疑入 §9 待复核 |
| 2026-09-06 | 3 | 引擎界面层 ✅ | 前任智能体因读 PNG 触发模型 400 终止(只支持文本),换新智能体从磁盘代码接手无损续作:修 apply_pickup 第三参契约错位、先攻怪接线(monsters_dead 台账判断,端到端差 80 钉死)、1005 守卫门自动弹开、存档门槛、api 扩 14 键、像素断言+无头主循环冒烟;引擎 51 测试、全量 166 绿(主线复验) |
| 2026-09-06 | 6 | 集成第一批 ✅ | `a955f34` 动态摆放持久化(placements)/杀怪触发器(keep_alive 永久取消)/跨层挂起(pending+进层钩子)/T 键道具/祭坛 AltarFlow/结局画面/事件排队;main.py 全量接 NpcFlow;12 条真数据集成测试(序章被夺→2 层监狱/49 层封印阵/圣水/魔杖/挂起重演/摆放存档/结局),178 绿 |
| 2026-09-06 | 6 | 机关语义物化 ✅ | `5325860` 语义侦察(只读智能体,上游 ParserFactory/DoorSystem 等 10+ 文件取证)→ 转换 v5:hide/appear/passive/monster_move 格子标记 + appear_event/disappear_event/door_unlocks/wall_shows 楼层字段;引擎接线全部机关;**修复守卫门按 1005 过滤的严重缺陷**(实际门 id 混用);f39 商人错位按贴图位勘误;7 条机关集成测试,186 绿 |
| 2026-09-06 | 6 | 全塔走查 ✅ | `scripts/walkthrough.py` 序章→1~49 全层→通关链(26 公主冰冻开路/24 传送门/50 小偷→真魔王)→ending,真实引擎一步一格,20 事件/105 怪/187 测试绿。走查途中**重大勘误 2 条**:①巫师 125/126 上游本无"不可战斗"字段(当初误标 unfightable 会让 48 层上梯永久锁死),转换 v6 删除标记;②事件 11(32 层魔王埋伏)appear/move 引用的怪 135 上游数据自身缺失,保持原样跳过并记 warning(30 层伐木工事件 12 同源)。机制事实修正 2 条:49 层杀十字警卫每格被对角警卫**夹击减半×4**,加上 40 层起巫师魔伤——原版通关链必须回宝库(事件 22:3 血瓶+3 红宝石+3 蓝宝石+骑士盾+神圣盾,神圣盾免疫巫师魔伤)补够数值才打得过 50 层真魔王,走查补上了这段;32 层踩 (5,9) 事件 11 会把 (10,0) 隐藏史莱姆当魔王走位挪到中轴,引擎按 hide 语义(不挡路)放行 |
| 2026-09-06 | 7 | 打磨收尾 ✅(开发侧) | `README.md`(快速开始/按键表/走查说明/架构图);异常路径 4 条测试已有覆盖(缺数据文件/坏 JSON/坏档×2/坏档菜单不崩);性能基准:全塔走查约 3000 步真实引擎 0.2 秒,逻辑层远低于 60FPS 帧预算,渲染帧率与操作手感留 §8 真机清单;`74a42d4`/`077a7d8` |

## 8. 运行验证清单(用户执行,替代"真机验证")

- [ ] `./run.sh` → 窗口打开,状态栏显示 400/10/10/0,开场越狱剧情后进入第 2 层
- [ ] 方向键/WASD 走格子;撞墙不动;撞怪自动战斗、血量扣减与怪物手册预测一致
- [ ] 40 层先攻怪:撞先攻位置的鬼战士,损血比普通位置多挨一刀(与预测一致)
- [ ] 捡钥匙/宝石/血瓶,状态栏数值按区域倍率正确变化(41 层宝石是 1 层的 5 倍)
- [ ] 无钥匙撞门:不动+提示;有钥匙:开门、钥匙 −1
- [ ] 怪物手册(H 键):显示怪物属性+预测损血/回合,实战结果与预测一致(抽查 2 只)
- [ ] 打不过的怪(开局撞石头人):移动被拒
- [ ] 巫师:走到相邻格扣 100/200;持神圣盾后免疫(40 层以上验证)
- [ ] 魔法警卫夹击:踏入两警卫中间格,HP 减半(奇数向上取整)
- [ ] 祭坛:价格按 20/40/80… 递增,全塔共享;三选一效果正确
- [ ] 存档/读档:拿到怪物手册前存档被拒(原版设定);拿到后可存,读档状态回滚正确
- [ ] 异常路径:手工把 save_1.json 改坏 → 读档报清晰错误不崩;删掉某层数据文件 → 启动时报缺哪个
- [ ] 特殊机关抽查:44 层用中心飞行器进入并拿到神圣盾;49 层破阵后假魔王变 1 成数值;50 层剧情战后真身可战胜
- [ ] 32 层魔王埋伏:踩中轴 (5,9) 触发事件 11——魔王狠话对话弹出(注:魔王走位演出因上游数据损坏缺失,§9 有说明),警卫机关与逃生路(杀两中级卫兵→西门弹开)可走
- [ ] 49 层封印→宝库→50 层决战链:破阵杀 4 十字警卫(每格被夹击 HP 减半,注意留血)→ 假魔王弱化击杀 → 宝库 11 件宝物全拿(3 血瓶回血)→ 50 层真魔王可战胜(残血不足时先别碰)
- [ ] 通关:击败真身 → 结局画面+仙子评分;游戏全程 60 FPS 无卡顿

## 9. 遗留风险与未验证点(诚实清单)

- **版权**:数据复制自 MIT 仓库(其内容本身仍是原作关卡设计的复制)→ 仅限个人学习、不公开分发、不商用;素材全自绘
- **不做**:二周目(怪物数值×44,通关即结局)——2026-09-06 用户已确认
- **先攻怪**:2026-09-06 用户重新拍板**做上**(此前因无权威数据确认不做;考古终审在 40 层数据找到 12 只确凿证据:鬼战士/战士/双手剑士/骑士各 3,位置驱动)。calc_battle 语义已实现并单测,引擎按楼层 first_attack 表接线中
- **存疑按惯例处理**:上/下飞行器做一次性(原版文档未明示);tacthgin 中镐与向上飞行器无获取途径(死道具)
- **平衡性**:数值 1:1 于原版,理论上可通关,但只能靠真玩验证——运行清单最后一项
- **结局无演出**:数据只到"杀死真魔王"为止,无结局动画/评分(忠实数据;想加演出属于超范围改动,默认不加)
- **祭坛价格两源不一致**:按设计文档二次曲线 10×(n²−n+2)=20/40/80/140(tswKai/exe 考据)实现;tacthgin 源码实为线性 20/40/60——**以 tswKai 为准**,真机验证时全塔买 4 次以上核价格
- **事件层语义存疑 4 条**(智能体按附录/简报字面实现,docstring 有标注,真机复核):炸弹"8 格圈"vs 参考实现"4 直邻"(取附录 B);fly_wand 目标含当前层且 ≠44("楼梯旁"校验留给引擎);specialMove/beAttack 为纯演出透传/不减血
- **47 层巫师镜像瞬移**:已接线(monster_move 标记,吃魔伤→瞬移对称格,单测钉死)
- **f39 商人摆放勘误(数据修正决策)**:tacthgin 里 NPC30 的属性位 (0,10) 与贴图位 (8,1) 错位(属性位隐形可对话、贴图位可穿行,两头都对不上原版)——转换时**按贴图位摆**,以最接近原版游戏体验为准;楼层 JSON 的 fix 字段留了勘误说明
- **两处小偏差(记录在案)**:①tacthgin 战斗元素格=贴图格+1 行(大怪),我们打在贴图格——对守卫门/杀怪触发无影响(它们都用贴图格);②镐能敲 door 层的 1006 门,参考实现只敲 wall 层——镐在本数据无获取途径(死道具),无实害
- **40 层隐藏楼梯的显现途径勘误**:是杀掉骑士队长 124 后事件 18 show 出来的(此前推测"镐破墙"不对);10 层隐藏上梯同理是事件 4 show(杀骷髅队长 107 触发)——都已是数据驱动的现成链路
- **巫师 unfightable 勘误(2026-09-06 走查推翻物化,重大)**:上游 monster.json 的 125/126 只有 magicAttack(相邻魔伤),没有任何"不可战斗"字段;当初误标 unfightable 会让 48 层上梯(唯一通路被初级巫师堵着)永远锁死。转换 v6 已删标记,battle 的 unfightable 分支保留作通用机制位(当前全塔无怪使用)
- **事件 11/12 引用不存在的怪 135(上游数据损坏)**:32 层魔王埋伏(事件 11)appear 的魔王 135 在上游 monster.json 查无此怪;勘误结论:保持原样,跳过 appear 并记 warning,事件 11 的 move 因此挪走的是 (10,0) 梯格里隐藏的史莱姆 100(引擎按 hide 语义不挡路放行,不影响通关)。真机到 32 层看不到"魔王现身走位"演出,但埋伏对话与机关完好
- **api_jump_floor(49) 会抛 LandingError(已知行为)**:49 层无上梯,跳层函数算不出落点——正常玩法(走到 49 再进)不受影响;走查脚本用手动摆位绕过。若后续工具/金手指要跳 49 层,需单独处理落点
- **49→50 层数值链(真机清单重点)**:49 层封印阵杀 4 只十字警卫,每格被对角警卫左右/上下**夹击 HP 减半**(走查实测 4 次);之后必须进宝库(杀假魔王→事件 22)拿 3 蓝血瓶(每瓶回 200×5=1000)+6 宝石+双盾回血提攻防,**否则 50 层真魔王(攻 1580)多半打不过**——真机验证按 §8 清单走这条链

---

## 附录 A:33 怪权威数值表(来源:原版 TSW.exe 数据段直接提取,经 4 个独立复刻交叉验证;已修正 tacthgin 的 3 处金币抄录错误)

| 怪物 | HP | 攻 | 防 | 金币 | 备注 |
|---|---|---|---|---|---|
| 绿色史莱姆 | 35 | 18 | 1 | 1 | |
| 红色史莱姆 | 45 | 20 | 2 | 2 | |
| 大史莱姆 | 130 | 60 | 3 | 8 | |
| 小蝙蝠 | 35 | 38 | 3 | 3 | |
| 初级法师 | 60 | 32 | 8 | 5 | |
| 高级法师 | 100 | 95 | 30 | 22 | |
| 初级卫兵 | 50 | 48 | 22 | 12 | |
| 中级卫兵 | 100 | 180 | 110 | 100 | |
| 骷髅人 | 50 | 42 | 6 | 6 | |
| 骷髅士兵 | 55 | 52 | 12 | 8 | |
| 骷髅队长 | 100 | 65 | 15 | 30 | 10 层区域头目 |
| 大蝙蝠 | 60 | 100 | 8 | 12 | |
| 兽人 | 260 | 85 | 5 | 18 | 十字架生效对象 |
| 兽人武士 | 320 | 120 | 15 | 30 | 十字架生效对象 |
| 石头人 | 20 | 100 | 68 | 28 | 高防门槛怪 |
| 魔王芝诺(显示值) | 8000 | 5000 | 1000 | 500 | 50 层,剧情战(不可战胜) |
| 魔法警卫 | 230 | 450 | 100 | 100 | 夹击机制+49 层封印阵 |
| 吸血鬼 | 444 | 199 | 66 | 144 | 20 层区域头目;十字架生效对象 |
| 大乌贼(头) | 1200 | 180 | 20 | 100 | 汉化版整体一场战斗;打完出魔镐 |
| 魔龙 | 1500 | 600 | 250 | 800 | 屠龙匕生效对象 |
| 骑士队长 | 120 | 150 | 50 | 100 | 38 层剧情战 |
| 骑士 | 160 | 230 | 105 | 65 | |
| 双手剑士 | 100 | 680 | 50 | 55 | 双剑=数值双攻 |
| 战士(士兵) | 210 | 200 | 65 | 45 | |
| 鬼战士 | 220 | 180 | 30 | 35 | |
| 幽灵 | 320 | 140 | 20 | 30 | |
| 大法师 | 4500 | 560 | 310 | 1000 | 25 层,不可正面战胜 |
| 吸血蝙蝠 | 200 | 390 | 90 | 50 | |
| 史莱姆王 | 360 | 310 | 20 | 40 | |
| 初级巫师 | 220 | 370 | 110 | 80 | 不可战斗;相邻格固定扣 100 |
| 高级巫师 | 200 | 380 | 130 | 90 | 不可战斗;相邻格固定扣 200 |
| 黑暗骑士 | 180 | 430 | 210 | 120 | |
| 高级卫兵 | 180 | 460 | 360 | 200 | |
| (汉化版魔王真身) | 5000 | 1580 | 190 | 500 | 50 层真身战,可战胜通关 |

## 附录 B:道具表(武器/盾为定值;血瓶/宝石按区域倍率 area = floor(层÷10)+1)

| 道具 | 效果 | 获取位置 |
|---|---|---|
| 红血瓶 | HP +50×area | 各层 |
| 蓝血瓶 | HP +200×area | 各层 |
| 红宝石 | 攻 +1×area | 各层 |
| 蓝宝石 | 防 +1×area | 各层 |
| 铁剑 / 银剑 / 骑士剑 / 圣剑 / 神圣剑 | 攻 +10 / +20 / +40 / +50 / +100 | 拾取 |
| 铁盾 / 银盾 / 骑士盾 / 圣盾 / 神圣盾 | 防 +10 / +20 / +40 / +50 / +100 | 神圣盾在 44 层;另免疫巫师魔法伤害 |
| 怪物手册 | 查属性+预测损血/回合;**持有才能存档** | 3 层老人 |
| 记事本 | 对白回看 | 6 层 |
| 飞行魔杖 | 楼梯旁跳任意已到过楼层;不限次;**不能去 44 层** | 1 层 |
| 镐 | 敲掉面前 1 格墙,一次性 | 16 层大乌贼后 |
| 地震卷轴 | 摧毁本层全部墙,一次性 | 39 层商人 4000 金币 |
| 冰冻魔法 | 冻结周围岩浆为平地 | 拾取 |
| 炸弹 | 炸死周围一圈非头目怪,一次性 | 拾取 |
| 圣水 | HP += 攻+防,一次性(越晚用越赚) | 14 层老人 |
| 幸运金币 | 杀怪金币 ×2 | 拾取 |
| 十字架 | 对兽人/兽人武士/吸血鬼攻 ×2 | 藏在 15 层以上的墙内 |
| 屠龙匕 | 对魔龙攻 ×2 | 拾取 |
| 上/下飞行器 | 直接上/下一层,一次性(存疑项,按复刻惯例) | 拾取 |
| 中心飞行器 | 飞到本层中心对称空地,3 次;进入 44 层的密宝 | 拾取 |
| 魔法黄钥 | 打开本层全部黄门,一次性 | 拾取 |

## 附录 C:NPC / 商店 / 机关速查

- **祭坛**:4/12/32/46 层,三选一(生命/攻/防),全塔共享次数,公式见 §3.1
- **商人**:6/7/12/15/31/38/39/45/47 层(卖钥匙、1000 金币买 4黄+1蓝等);28 层 100 金币/把回收黄钥匙;34 层 1000 金币→+2000 HP;39 层 4000 金币卖地震卷轴;另有祝福商人免费攻防各 +3%
- **老人**:3 层送怪物手册、14 层送圣水、另有送 1000 金币者,其余为提示
- **小偷**:开局带你越狱;16 层大乌贼前、35 层魔龙前帮你开暗道
- **仙子**:每打完一个区域头目(10/20/25(大法师区)/38/50 层)出现评分(按生命+攻+防+钥匙)
- **44 层异空间**:楼梯/飞行魔杖均不可达,只能用中心飞行器飞入;神圣盾藏于此
- **49 层封印**:4 个魔法警卫排"骰子5"阵,破阵后假魔王数值 ×0.1
- **50 层**:魔王芝诺剧情战(显示值,不可战胜)→ 汉化版真身战(5000/1580/190)→ 通关
